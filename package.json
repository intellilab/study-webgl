{
  "name": "study-webgl",
  "version": "0.0.0",
  "private": true,
  "description": "",
  "author": "",
  "license": "ISC",
  "scripts": {
    "dev": "plaid develop",
    "prebuild": "npm run lint",
    "build": "plaid build",
    "lint": "eslint --ext .js,.vue .",
    "analyze": "cross-env RUN_ENV=analyze npm run build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "engines": {
    "node": ">=8"
  },
  "husky": {
    "hooks": {
      "pre-push": "yarn lint"
    }
  },
  "devDependencies": {
    "@babel/preset-react": "^7.0.0",
    "@gera2ld/plaid": "^1.1.2",
    "gh-pages": "^2.0.1",
    "husky": "^1.3.1"
  },
  "dependencies": {
    "@babel/runtime": "^7.3.4",
    "@gera2ld/jsx-dom": "^0.1.1",
    "@gera2ld/rem": "^0.1.2",
    "core-js": "^2.6.5",
    "three": "^0.102.1"
  }
}
